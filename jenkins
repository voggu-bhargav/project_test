pipeline {
    agent any
    stages {
        stage('run docker compose') {
            steps {
                sh 'docker-compose up'
                sh 'docker-compose down'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
