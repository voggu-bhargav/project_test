pipeline {
    agent any
    stages {
        stage('run docker compose') {
            steps {
                sh 'docker-compose up -d'
                sh ' sleep 30 '
                sh 'docker-compose down'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
