pipeline {
    agent any
    stages {
        stage('run docker compose') {
            steps {
                sh 'docker-compose up'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
