pipeline {
    agent any
    stages {
        stage('run docker compose') {
            steps {
                sh 'docker-compose rm up'
                sh 'docker-compose down'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
