pipeline {
    agent any
    stages {
        stage('run docker compose') {
            steps {
                sh 'docker-compose run --rm'
                sh 'docker-compose down'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
